/**
 * While creating a new class we should ensure that the class name
 * of the passing class match the created classes;
 * first we should perform a fetch request for checking existing
 * classes name in the database when we see the first typed character
 * we autocomplete with some useful data
 * 
 * we can use a very simple Machine learning model that will learn
 * behaviour and predict the next classes the user tries to create
 * for the subjects too 
 */

