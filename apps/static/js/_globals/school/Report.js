import domain from '../domain.js'

const UtilApiURLs = {
    SessionReportPerClass:`${domain}/generate_session_reports/`,
    GenerateExcelReports:`${domain}/generate_excel_reports/`,
};

export default UtilApiURLs;
