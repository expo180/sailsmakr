import domain from '../domain.js'

const UtilApiURLs = {
    DeleteSchoolAdminUrl: `${domain}/auth/manage_admins`,
    GetStudentGrades: `${domain}/classrooms/v1/manage_grades`

};

export default UtilApiURLs;
