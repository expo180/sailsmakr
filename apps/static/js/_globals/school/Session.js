import domain from '../domain.js'

const UtilApiURLs = {
    UpdateSessionStatusUrl: `${domain}/sessions/v1/update-session-status/`,
    MigrateSessionData: `${domain}/sessions/v1/migrate-to-a-new-session/`
};

export default UtilApiURLs;
